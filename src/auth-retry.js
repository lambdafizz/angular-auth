(function (angular) {


// lf.auth.retry
// -------------
//
// This modules purpose is to provide easy way to retry
// failed responses. It's initial goal was to ease work with
// expiring tokens (like OAuth), but it can be used in many
// generic ways (like reacting to network/backend failures
// etc).

angular.module('lf.auth.retry', []);


})(window.angular);
